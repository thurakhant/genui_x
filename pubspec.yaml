name: genui_x
description: "Connect any LLM — Claude, OpenAI, Gemini, Ollama, or a custom proxy — to Google's genui (Generative UI) framework with one Transport."
version: 0.1.0
repository: https://github.com/thurakhant/genui_x
issue_tracker: https://github.com/thurakhant/genui_x/issues
homepage: https://github.com/thurakhant/genui_x
documentation: https://pub.dev/documentation/genui_x/latest/

# Up to 5 topics. Picked for pub.dev discoverability around the multi-provider
# generative-UI niche. `flutter` is implied by the SDK constraint, so we use
# the slot for a provider keyword instead.
topics:
  - genui
  - llm
  - claude
  - gemini
  - openai

# Screenshots show up on the pub.dev package page. Each path must exist or
# `dart pub publish` and CI's `flutter analyze --fatal-infos` will fail.
# Uncomment in the SAME commit that adds screenshots/demo.gif (<4 MB) —
# publishing 0.1.0 waits for that commit.
#
# screenshots:
#   - description: "genui_x rendering an AI-generated card from a chat prompt."
#     path: screenshots/demo.gif

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.35.7"

dependencies:
  flutter:
    sdk: flutter
  # genui 0.10.0 stopped re-exporting A2uiMessage; it now lives in a2ui_core.
  a2ui_core: ^0.0.1-dev002
  genui: ^0.10.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
