name: genui_x_example
description: Example app demonstrating the genui_x package.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  genui: ^0.10.0
  json_schema_builder: any
  genui_x:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
